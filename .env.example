APP_ENV=development
APP_VERSION=1.1.25
DB_HOST=localhost
DB_NAME=gcustos
DB_USER=root
DB_PASS=

# Observação:
# - Copie para .env em desenvolvimento (arquivo já está ignorado no Git).
# - NÃO comite segredos. Em produção, prefira definir variáveis via painel do host
#   ou usando SetEnv no Apache do servidor.